# commerce 계층 (v2 자리 — MVP에서 구현하지 않음)

7-1장 인쇄/포토북 주문(v2 유료) 모듈이 들어올 **격리된 공간**.

- MVP에서는 코드 없음. 구조적 자리만 비워둠.
- v2에서 추가될 것:
  - `Order`, `ShippingAddress` 데이터 모델 (7-1장 초안)
  - 제휴 인쇄사 API 연동(주문 위임 — 직접 물류/인쇄 안 함)
  - 외부 PG 결제(실물 상품), 개인정보 제3자 제공 동의 처리

## MVP에서 미리 대비할 것 (다른 계층에서 보장)
- **원본 해상도 보존:** `Capture` 원본을 인쇄 가능 고해상도로 저장(썸네일과 분리). → `data/` 계층에서 보장.
- **계정 연결:** 주문은 `Account`에 귀속 → 로그인/계정 구조를 주문과 연결 가능하게(작업 #7).
